{
  "name": "mgt",
  "version": "0.8.1",
  "description": "Search and browse the WildFly management model across versions and feature packs",
  "mcpServers": {
    "wildfly-model-graph": {
      "command": "npx",
      "args": ["--yes", "@model-graph-tools/mcp-server@0.8.1"]
    }
  }
}
