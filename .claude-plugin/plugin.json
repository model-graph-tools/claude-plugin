{
  "name": "mgt",
  "version": "0.1.0",
  "description": "Search and browse the WildFly management model across versions and feature packs",
  "mcpServers": {
    "wildfly-model-graph": {
      "command": "npx",
      "args": ["--yes", "@model-graph-tools/mcp-server@latest"]
    }
  }
}
